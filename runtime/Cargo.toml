[package]
name = "runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
contract = { path = "../contract" }
thiserror = "2"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
