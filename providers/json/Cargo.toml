[package]
name = "json-provider"
version = "0.1.0"
edition = "2024"

[dependencies]
contract = { path = "../../contract" }
runtime = { path = "../../runtime" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
