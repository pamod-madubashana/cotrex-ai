[package]
name = "example-runner"
version = "1.0.0"
edition = "2024"
publish = false

[dependencies]
contract = { path = "../contract" }
runtime = { path = "../runtime" }
mock = { path = "../providers/mock" }
