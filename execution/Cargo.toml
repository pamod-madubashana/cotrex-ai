[package]
name = "execution"
version = "1.0.0"
edition = "2024"

[dependencies]
kernel = { path = "../kernel" }
thiserror = "2"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
tempfile = "3"
