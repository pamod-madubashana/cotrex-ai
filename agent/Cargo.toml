[package]
name = "agent"
version = "1.0.0"
edition = "2024"

[dependencies]
execution = { path = "../execution" }
kernel = { path = "../kernel" }
uuid = { version = "1", features = ["v4"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
